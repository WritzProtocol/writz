export type LiquidGoldTone = "gold" | "silver";

export interface LiquidGoldPalette {
  deep: string;
  bronze: string;
  base: string;
  highlight: string;
  brightest: string;
  shadowMix: string;
  dropShadow: string;
}
