# Writz landing (writz.xyz). Copy to .env.local and adjust as needed.

# Canonical site URL, used for metadataBase, OpenGraph, robots and sitemap.
NEXT_PUBLIC_SITE_URL=https://writz.xyz

# Where the "Launch App" buttons point.
NEXT_PUBLIC_APP_URL=https://testnet.writz.xyz

# Umami pageview analytics website ID. Leave empty to disable tracking.
NEXT_PUBLIC_UMAMI_WEBSITE_ID=
