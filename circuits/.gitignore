/node_modules/
/build/
/ptau/
# proving keys are large; the served copies live in frontend/public/circuits/
keys/*.zkey

# Production ceremony: intermediate/final .zkey files are large
# binaries — publish them as GitHub Release assets instead (see
# scripts/ceremony/README.md). The transcript, contract-shape vkey, and
# manifest ARE committed (small, and are the actual audit trail).
/ceremony/ptau/
ceremony/*/*.zkey
